import React from 'react';
import TopTabNavigator from './MainAppNavigator';

// Export the TopTabNavigator as MainTabNavigator for compatibility
const MainTabNavigator = TopTabNavigator;

export default MainTabNavigator;
